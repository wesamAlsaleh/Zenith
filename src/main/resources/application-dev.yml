spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
#  config:
#    import: optional:file:.env[.properties]  # This tells Spring to load the .env file and [.properties] to tell Spring to parse the file using the standard Key=Value property format
  jpa:
    show-sql: true